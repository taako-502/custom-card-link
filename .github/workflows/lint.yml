name: Lint Check

on:
  push:
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Volta
        uses: volta-cli/action@v5

      - name: Run Lint
        run: |
          yarn
          yarn lint
